name: Check new releases

on:
  push:
    branches: [ "main" ]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: My last release
        id: last_release
        uses: InsonusK/get-latest-release@v1.0.1
        with:
          myToken: ${{ github.token }}
          exclude_types: "draft|prerelease"
          view_top: 1
      - name: "Print result"
        run: |
          echo "id: ${{ steps.last_release.outputs.id }}"
          echo "name: ${{ steps.last_release.outputs.name }}"
          echo "tag_name: ${{ steps.last_release.outputs.tag_name }}"
      - name: Check LLVM versions
        uses: actions/github-script@v6
        with:
          github-token: ${{ github.token }}
          script: |
            const { execute } = require('child_process');

            var latest_version="${{ steps.last_release.outputs.name }}";
            if (latest_version === "") {
              latest_version = "18.1.0";
            }

            // Function to compare two version strings
            function compareVersions(a, b) {
              const partsA = a.split('.');
              const partsB = b.split('.');
              const maxLength = Math.max(partsA.length, partsB.length);

              for (let i = 0; i < maxLength; i++) {
                const numA = parseInt(partsA[i]) || 0;
                const numB = parseInt(partsB[i]) || 0;
                if (numA !== numB) {
                  return numA - numB;
                }
              }
              return 0;
            }

            var versions_to_trigger = [];

            const options = {};
            options.listeners = {
              stdout: (data: Buffer) => {
                // Split the output into an array of lines
                const lines = data.toString().trim().split('\n');
              
                for(const line in lines) {
                  // Split the line into oid and ref
                  const [oid, ref] = line.split('\t');
              
                  // Remove prefix from ref
                  const version = ref.replace('refs/tags/llvmorg-', '');
              
                  // Check if version consists only of numbers and dots
                  if (/^[0-9.]+$/.test(version)) {
                    // Compare versions
                    if (compareVersions(version, latest_version) > 0) {
                      console.log(version + " is greater, need to build binaries");

                      versions_to_trigger.push(version)
                    }
                  }
                }
              },
              stderr: (data: Buffer) => {
                console.error(`stderr: ${data.toString()}`);
              }
            };

            await exec.exec('git', ['--tags', '--refs', '--sort=-version:refname', 'https://github.com/llvm/llvm-project', 'llvmorg-*'], options);


        